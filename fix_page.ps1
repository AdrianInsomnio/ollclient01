$content = Get-Content "C:\Users\hhilo\OneDrive\Desktop\EntornoDev\ollclient\app\workstation\user\cola\page.tsx.fixed" -Raw; $content = $content -replace "Ã³", "ó"; $content = $content -replace "Ã¡", "á"; $content = $content -replace "Ã©", "é"; $content = $content -replace "Ã­", "í"; $content = $content -replace "Ãº", "ú"; $content = $content -replace "Ã±", "ñ"; $content = $content -replace "Ã¼", "ü"; $content = $content -replace "ï¿½", "ó"; $content = $content -replace "ï¿½", "á"; $content = $content -replace "ï¿½", "é"; $content = $content -replace "ï¿½", "í"; $content = $content -replace "ï¿½", "ú"; $content = $content -replace "ï¿½", "ñ"; $content = $content -replace "ï¿½", "ü"; $content = $content -replace "Â·", "·"; $old = "import { Card, CardContent, CardHeader, CardTitle } from"; $new = "import { Card, CardContent, CardHeader, CardTitle } from`nimport { AgregarAColaModal } from`nimport { queryClient } from"; $content = $content -replace [regex]::Escape($old), $new; $content = $content -replace "export default function ColaPage() {", "function invalidateConsultations() {`n  queryClient.invalidateQueries({ queryKey: [`"consultations-open`"] })}`n  queryClient.invalidateQueries({ queryKey: [`"appointments-today`"] })}`n`nexport default function ColaPage() {"; Set-Content "C:\Users\hhilo\OneDrive\Desktop\EntornoDev\ollclient\app\workstation\user\cola\page.tsx" -Value $content -Encoding UTF8; Write-Host "done"
